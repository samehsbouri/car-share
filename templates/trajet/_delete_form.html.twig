
<form method="post" action="{{ path('app_trajet_delete', {'id': trajet.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce trajet ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ trajet.id) }}">
    <button class="btn btn-danger btn-lg rounded-pill w-100">
        <i class="fas fa-trash-alt me-2"></i>
    </button>
</form>